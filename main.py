from gui.app_window import AppWindow

if __name__ == "__main__":
    # 1. Instanciar la ventana principal de la interfaz gráfica
    app = AppWindow()
    
    # 2. Iniciar el bucle principal para mantener la aplicación ejecutándose
    app.mainloop()